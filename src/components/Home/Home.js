import React, { useRef } from "react"
import { useHistory } from "react-router-dom"


const Home = props => {

    return (
        <main style={{textAlign:"center"}}>
            <p>GG</p>
        </main>
    )
}
 export default Home